# Changelog

All notable changes to ClawBridge should be recorded here.

The project uses Semantic Versioning:
- `PATCH` for backward-compatible fixes
- `MINOR` for backward-compatible features
- `MAJOR` for breaking changes

## [Unreleased]

- Centralized runtime version reporting from `package.json`
- Added release/versioning guidance for tagged installs

## [0.1.0] - 2026-03-10

- Initial tracked release baseline for the current repo line
