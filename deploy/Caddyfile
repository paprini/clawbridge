{$A2A_DOMAIN:localhost} {
  encode zstd gzip

  header {
    -Server
    X-Content-Type-Options nosniff
    Referrer-Policy no-referrer
  }

  reverse_proxy a2a:9100

  # Rate limiting at proxy level
  @ratelimit {
    path /a2a
  }

  log {
    output stdout
    format json
  }
}
